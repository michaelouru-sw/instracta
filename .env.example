VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_WEB3FORMS_KEY=
# Optional: lets "Generate Outline" call a real model instead of the
# built-in template generator. Only used client-side for your own testing —
# move this server-side before shipping to real users.
VITE_OPENAI_API_KEY=
